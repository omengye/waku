// Metro resolves the platform files in mobile builds. This export only gives
// TypeScript a concrete module when it checks imports without a platform.
export { RenameDialog } from './rename-dialog.ios';
